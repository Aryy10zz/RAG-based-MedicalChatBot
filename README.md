# RAG-based-MedicalChatBot
This project is a RAG based Medical Chatbot trained using a Medical Database from HuggingFace. I have used ChromaDB as a vector database to store the information. I have used the LLamaIndex to build the RAG pipeline and GeminiFlash API.
